<%
if params[:style] == 'none'
  thumb = @object.file
else
  w = params[:w].blank? ? 0 : params[:w]
  h = params[:h].blank? ? 0 : params[:h]
  thumb = @object.file.thumb("#{w}x#{h}#{params[:style]}")
end
%>{
  "html": "<%= j(image_tag(thumb.url, alt: @object.alt, width: thumb.width, height: thumb.height)) %>"
}
