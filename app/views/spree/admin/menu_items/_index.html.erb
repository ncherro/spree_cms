<% content_for :page_title do %>
  <%= "for &ldquo;#{@menu.title}&rdquo;".html_safe if @menu %>
<% end %>

<ul class="cms-menu">
  <%= render_menu_tree(@menu) do |node|
    link_to("hey!", '/')
  end
  %>
</ul>
